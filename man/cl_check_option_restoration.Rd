% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-option-restoration.R
\name{cl_check_option_restoration}
\alias{cl_check_option_restoration}
\title{Check for unrestored par()/options()/setwd() changes}
\usage{
cl_check_option_restoration(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags a \code{par()}, \code{options()}, or \code{setwd()} call in \verb{R/} that changes
state without a paired \code{on.exit()} call in the same function frame to
restore it. Per the CRAN Cookbook, packages shouldn't permanently
change a user's graphical parameters, options, or working directory;
if a function has to, it should save the old value and restore it with
\code{on.exit()} immediately.
}
\details{
\code{setwd()} always counts as a change. For \code{par()}/\code{options()}, a bare
call with no arguments, or a call whose only arguments are unnamed
(e.g. \code{par("mfrow")}, or \code{par(oldpar)}/\code{options(oldopts)} restoring a
previously-saved list) is treated as a query/restore rather than a
change and isn't flagged; \code{par(no.readonly = TRUE)} is also excluded
as it's the standard snapshot-for-later-restore idiom, not a change by
itself. Any other call with at least one named argument is treated as
a change.

This is a coarse heuristic in two ways worth knowing about: it doesn't
check that the \code{on.exit()} call actually restores the \emph{same} thing
that was changed (any \code{on.exit()} anywhere in the same function frame
counts), and it doesn't check that the restore happens immediately
after the change, only that both are present in the same frame. A
change with no enclosing function at all (so \code{on.exit()} couldn't
apply even if present) is always flagged.
}
