% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-pkg.R
\name{cl_example_pkg}
\alias{cl_example_pkg}
\title{Scaffold a minimal package on disk for trying out cranlint checks}
\usage{
cl_example_pkg(description = character(), r_files = list(), man_files = list())
}
\arguments{
\item{description}{A named character vector of DESCRIPTION fields to
add or override on top of a minimal default (\code{Package}, \code{Title},
\code{Version}, \code{Authors@R}, \code{Description}, \code{License}).}

\item{r_files}{A named list mapping a filename (e.g. \code{"foo.R"}) to a
character vector of lines, written under \verb{R/}.}

\item{man_files}{A named list mapping a filename (e.g. \code{"foo.Rd"}) to
a character vector of lines, written under \verb{man/}.}
}
\value{
The path to the scaffolded package (a tempdir). Callers should
\code{unlink(path, recursive = TRUE)} once done with it.
}
\description{
Writes a valid, minimal package (a \code{DESCRIPTION} file, and optionally
\verb{R/} and \verb{man/} files) to a new temporary directory. This exists to
support the runnable \verb{@examples} on \verb{cl_check_*()}/\code{lint_cran()}, and
to make it easy to try cranlint out interactively, without needing a
real package on disk -- it is not part of the actual linting logic.
}
\examples{
pkg_dir <- cl_example_pkg(
  description = c(Description = "Does a thing."),
  r_files = list(simulate.R = c(
    "simulate <- function() {",
    "  set.seed(42)",
    "  rnorm(1)",
    "}"
  ))
)
lint_cran(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
