% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint-cran.R
\name{lint_cran}
\alias{lint_cran}
\title{Run all cranlint checks against a package}
\usage{
lint_cran(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract (see
\code{AGENTS.md}), combining every check's findings. Zero rows if no check
reports any findings.
}
\description{
Runs every \verb{cl_check_*()} check (DESCRIPTION issues like title case and
Authors@R formatting, and code/doc issues like hardcoded seeds and
\code{.GlobalEnv} writes) against \code{path} and combines their results into a
single tibble. This is cranlint's single top-level entry point,
mirroring the role \code{lintr::lint()} plays for that package.
}
\details{
If a check errors -- for example, because \code{path} has no \code{DESCRIPTION}
file at all -- that error propagates rather than being caught and
turned into a result row, since it signals something more fundamental
than an individual finding. A single unparseable R file, by contrast,
is already handled gracefully (skipped with a warning) and does not
stop the other checks from running.
}
\examples{
pkg_dir <- cl_example_pkg(
  description = c(Description = "Does a thing."),
  r_files = list(simulate.R = c(
    "simulate <- function() {",
    "  set.seed(42)",
    "  rnorm(1)",
    "}"
  ))
)
lint_cran(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
