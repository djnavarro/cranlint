% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-warn-suppression.R
\name{cl_check_warn_suppression}
\alias{cl_check_warn_suppression}
\title{Check for options(warn = \if{html}{\out{<negative>}})}
\usage{
cl_check_warn_suppression(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags \code{options()} calls that set \code{warn} to a negative value, which
globally suppresses all warnings for the rest of the session and can't
be limited to a specific expression. Per the CRAN Cookbook, this is not
allowed even when the option is immediately restored afterwards;
\code{suppressWarnings()} around the specific expression is the recommended
replacement.
}
\examples{
pkg_dir <- cl_example_pkg(
  r_files = list(foo.R = c(
    "quietly <- function() {",
    "  options(warn = -1)",
    "}"
  ))
)
cl_check_warn_suppression(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
