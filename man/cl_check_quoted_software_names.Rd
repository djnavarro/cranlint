% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-quoted-software-names.R
\name{cl_check_quoted_software_names}
\alias{cl_check_quoted_software_names}
\title{Check that the package's own dependencies are quoted in Title/Description}
\usage{
cl_check_quoted_software_names(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags a package name from this package's own \code{Depends}/\code{Imports}/
\code{Suggests}/\code{LinkingTo} fields (excluding \code{"R"} itself) that appears
unquoted in the \code{Title} or \code{Description} field. Per the CRAN Cookbook,
software and package names should be wrapped in single quotes so the
automatic spell check doesn't flag them.
}
\details{
The candidate list is deliberately narrow: rather than a general list
of known software/API names (high false-positive risk -- see
\code{.agents/PLAN.md}), it's limited to packages this package actually
declares a dependency on, which keeps precision high. It also means the
check under-reports (e.g. it won't catch an unquoted "Python"). A plain
word matching a dependency's name doesn't always refer to the package
either, so findings are advisory rather than a firm "fix this."
}
\examples{
pkg_dir <- cl_example_pkg(
  description = c(
    Description = paste(
      "Wraps jsonlite for convenient parsing.",
      "It has no other dependencies."
    ),
    Imports = "jsonlite"
  )
)
cl_check_quoted_software_names(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
