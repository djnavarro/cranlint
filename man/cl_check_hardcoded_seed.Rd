% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-hardcoded-seed.R
\name{cl_check_hardcoded_seed}
\alias{cl_check_hardcoded_seed}
\title{Check for hardcoded random seeds in package code}
\usage{
cl_check_hardcoded_seed(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags \code{set.seed()} calls in \verb{R/} that pass a literal, unchangeable
number (e.g. \code{set.seed(42)}, \code{set.seed(seed = 42L)}, \code{set.seed(-5)}) as
any argument. Setting a seed a user can't override or opt out of, inside
a function, is a common CRAN rejection reason. Calls in \verb{tests/},
\verb{\\examples}, \verb{vignettes/}, and demos are out of scope -- and not scanned
at all, since checks only look at \verb{R/} -- because setting a seed there
is expected and recommended for reproducibility.
}
\examples{
pkg_dir <- cl_example_pkg(
  r_files = list(simulate.R = c(
    "simulate <- function() {",
    "  set.seed(42)",
    "  rnorm(1)",
    "}"
  ))
)
cl_check_hardcoded_seed(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
