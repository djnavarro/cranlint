% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-global-env-write.R
\name{cl_check_global_env_write}
\alias{cl_check_global_env_write}
\title{Check for writes to enclosing/global environments via <<- or ->>}
\usage{
cl_check_global_env_write(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags \verb{<<-}/\verb{->>} usage in \verb{R/}. This operator writes to the nearest
enclosing environment with an existing binding of the same name, or to
\code{.GlobalEnv} if none exists -- which the CRAN Repository Policy
explicitly forbids. Package code that only uses \verb{<<-} to update a
variable in a known parent scope (e.g. a closure factory) is technically
safe, but this check can't distinguish that case from an accidental
global write without deeper scope analysis, so every use is flagged for
review.
}
\examples{
pkg_dir <- cl_example_pkg(
  r_files = list(foo.R = c(
    "set_flag <- function() {",
    "  flag <<- TRUE",
    "}"
  ))
)
cl_check_global_env_write(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
