% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-dontrun-usage.R
\name{cl_check_dontrun_usage}
\alias{cl_check_dontrun_usage}
\title{Check for \dontrun{} usage in .Rd examples}
\usage{
cl_check_dontrun_usage(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags every line containing \verb{\dontrun\{} in a package's \verb{man/*.Rd}
files. This is a soft, review-level finding rather than a hard fail:
\verb{\dontrun{}} is a legitimate way to mark example code that genuinely
can't be run during checking (e.g. it needs credentials, a network
resource, or is illustrative pseudo-code), but it's also easy to
reach for out of habit for code that could run fine, or run via
\verb{\donttest{}} instead (which CRAN does run, just not during every
regular check) -- and code inside \verb{\dontrun{}} is never executed by
\verb{R CMD check}, so it can silently rot. Static analysis can't tell
which case applies, so every occurrence is surfaced for a human to
judge.
}
\details{
Matching is a plain line-based text search for the literal \verb{\dontrun\{}
markup within an \verb{\\examples\{\}} block, not a full Rd parse -- it
doesn't distinguish an active \verb{\dontrun{}} from one that's commented
out with \verb{\%}, and reports one finding per line containing the markup
rather than per \verb{\dontrun{}} block. The search is deliberately scoped
to \verb{\\examples\{\}} (see \code{.cl_examples_lines()}) so that prose elsewhere
in the page that merely \emph{mentions} \verb{\dontrun{}} -- e.g. a \verb{@details}
tag discussing the markup, as this very function's own documentation
does -- isn't mistaken for actual usage.
}
