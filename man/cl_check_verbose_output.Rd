% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-verbose-output.R
\name{cl_check_verbose_output}
\alias{cl_check_verbose_output}
\title{Check for print()/cat() calls outside print/format/summary methods}
\usage{
cl_check_verbose_output(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags a \code{print()} or \code{cat()} call in \verb{R/} that isn't nested inside a
function whose name looks like a \verb{print.*}/\verb{format.*}/\verb{summary.*} S3
method. Per the CRAN Cookbook, these produce console output a user
can't suppress; CRAN's own exception is printing inside \code{print},
\code{summary}, and similar methods, where it's the whole point of the
function.
}
\details{
Two things this check can't detect, so it will over-report relative to
what CRAN actually requires: a \code{cat()} call writing to a file/connection
rather than the console (e.g. \code{cat(x, file = "out.txt")}) is still
flagged, and the Cookbook's other accepted mitigation -- gating the
call behind a \code{verbose} argument, e.g. \code{if (verbose) cat(...)} -- isn't
recognized as an exemption. Both are worth a manual look before
deciding a finding is a real problem.
}
\examples{
pkg_dir <- cl_example_pkg(
  r_files = list(foo.R = c(
    "process <- function(x) {",
    "  cat(\"processing...\\\\n\")",
    "  x",
    "}"
  ))
)
cl_check_verbose_output(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
