% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-quoted-function-names.R
\name{cl_check_quoted_function_names}
\alias{cl_check_quoted_function_names}
\title{Check for quoted function names in Title/Description}
\usage{
cl_check_quoted_function_names(path = ".")
}
\arguments{
\item{path}{Path to the package root. Defaults to the current directory.}
}
\value{
A tibble following the cranlint check-result contract; see
\code{AGENTS.md}.
}
\description{
Flags a single-quoted word in the \code{Title} or \code{Description} field that
matches the name of a function defined in this package's own \verb{R/} files,
or an exported function from \code{base}, \code{stats}, \code{utils}, or \code{methods}.
CRAN's single-quote convention is for software/package names, not
function names; quoting a function name is a common sign that the wrong
thing got quoted (e.g. \code{'summary'} where a software name was meant).
}
\examples{
pkg_dir <- cl_example_pkg(
  description = c(
    Description = paste(
      "Provides a fast 'summary' method.",
      "It has no other dependencies."
    )
  )
)
cl_check_quoted_function_names(pkg_dir)
unlink(pkg_dir, recursive = TRUE)
}
